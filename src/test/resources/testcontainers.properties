# Project-level Testcontainers defaults (committed to git, no machine-specific paths).
# Docker socket path is configured per-developer in ~/.testcontainers.properties (NOT in git).
ryuk.disabled=true
